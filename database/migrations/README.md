# Database Migrations

Alembic revisions are added here after the SQLAlchemy metadata foundation is created. Migrations must preserve append-only regulated-event history and must not introduce destructive event mutation paths.
