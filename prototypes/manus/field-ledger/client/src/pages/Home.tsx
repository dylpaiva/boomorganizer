/** Field Ledger design reminder: Home is a full operational canvas—never a generic centered landing page. */
import WorkflowApp from "@/components/WorkflowApp";

export default function Home() {
  return <WorkflowApp />;
}
